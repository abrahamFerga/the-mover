# Auto-detect text files and normalize line endings to LF in the repo
* text=auto eol=lf

# Windows scripts must use CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Binaries
*.png binary
*.ico binary
*.dll binary
*.exe binary
