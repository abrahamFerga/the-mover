<Window x:Class="TheMover.App.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="The Mover — Settings"
        Width="480" Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="Break Schedule">
                <StackPanel Margin="12">

                    <TextBlock Text="Micro-break" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Interval (minutes, 1–240)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 x:Name="MicroIntervalBox"
                                 HorizontalContentAlignment="Center" />
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Duration (seconds, 10–600)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="2" Grid.Column="1"
                                 x:Name="MicroDurationBox"
                                 HorizontalContentAlignment="Center" />
                    </Grid>

                    <TextBlock Text="Long break" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Interval (minutes, 1–240)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 x:Name="LongIntervalBox"
                                 HorizontalContentAlignment="Center" />
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Duration (seconds, 10–600)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="2" Grid.Column="1"
                                 x:Name="LongDurationBox"
                                 HorizontalContentAlignment="Center" />
                    </Grid>

                    <CheckBox x:Name="AutoStartBox"
                              Content="Start with Windows"
                              Margin="0,4,0,0" />

                </StackPanel>
            </TabItem>
            <TabItem Header="Outlook">
                <StackPanel Margin="8">
                    <TextBlock Text="Outlook calendar integration settings will appear here (Epic 4)."
                               Foreground="Gray" FontStyle="Italic" />
                </StackPanel>
            </TabItem>
            <TabItem Header="About">
                <StackPanel Margin="8" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="The Mover" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock Text="Break reminder for desk workers." HorizontalAlignment="Center" />
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" IsDefault="True"
                    Margin="0,0,8,0" Click="Save_Click" />
            <Button Content="Cancel" Width="80" IsCancel="True"
                    Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
