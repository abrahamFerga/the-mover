<Window x:Class="TheMover.App.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="The Mover — Settings"
        Width="480" Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="Break Schedule">
                <StackPanel Margin="12">

                    <TextBlock Text="Micro-break" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Interval (minutes, 1–240)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 x:Name="MicroIntervalBox"
                                 HorizontalContentAlignment="Center" />
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Duration (seconds, 10–600)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="2" Grid.Column="1"
                                 x:Name="MicroDurationBox"
                                 HorizontalContentAlignment="Center" />
                    </Grid>

                    <TextBlock Text="Long break" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Interval (minutes, 1–240)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 x:Name="LongIntervalBox"
                                 HorizontalContentAlignment="Center" />
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Duration (seconds, 10–600)"
                                   VerticalAlignment="Center" />
                        <TextBox Grid.Row="2" Grid.Column="1"
                                 x:Name="LongDurationBox"
                                 HorizontalContentAlignment="Center" />
                    </Grid>

                    <CheckBox x:Name="AutoStartBox"
                              Content="Start with Windows"
                              Margin="0,4,0,0" />

                </StackPanel>
            </TabItem>
            <TabItem Header="Exercises">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Exercise library" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ListBox Grid.Row="1" x:Name="ExercisesList" SelectionMode="Single">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="2,4">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Instruction}" TextWrapping="Wrap"
                                               Foreground="Gray" FontSize="11" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
            <TabItem Header="Outlook">
                <StackPanel Margin="12">
                    <TextBlock Text="Microsoft 365 calendar" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,10"
                               Text="When connected, breaks are suppressed while a calendar event is active. Requires an Azure AD App Registration with Calendars.Read scope." />

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Tenant ID" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TenantIdBox" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Client ID" VerticalAlignment="Center" />
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="ClientIdBox" />
                    </Grid>

                    <TextBlock x:Name="CalendarStatusLabel"
                               Text="Status: Not connected"
                               Foreground="Gray" FontSize="11" Margin="0,0,0,8" />

                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="ConnectButton"
                                Content="Connect Outlook"
                                Width="130" Height="30"
                                Margin="0,0,8,0"
                                Click="Connect_Click" />
                        <Button x:Name="DisconnectButton"
                                Content="Disconnect"
                                Width="100" Height="30"
                                IsEnabled="False"
                                Click="Disconnect_Click" />
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="About">
                <StackPanel Margin="8" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="The Mover" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock Text="Break reminder for desk workers." HorizontalAlignment="Center" />
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" IsDefault="True"
                    Margin="0,0,8,0" Click="Save_Click" />
            <Button Content="Cancel" Width="80" IsCancel="True"
                    Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
